This repository contains my solutions to various MySQL challenges.
The goal is to practice SQL queries, improve database problem-solving skills, and build a reference for common query patterns.

📂 Repository Structure

Each solution is written in SQL (.sql files).

The problems are based on common MySQL coding exercises, including:

✅ SELECT statements

✅ JOIN (INNER, LEFT, RIGHT)

✅ Aggregation with GROUP BY, HAVING

✅ String functions (LIKE, REGEXP)

✅ Subqueries and Nested Queries

✅ Window Functions (where applicable)

🚀 How to Use

Clone this repository:

git clone https://github.com/your-username/mysql-challenges.git
cd mysql-challenges


Import the challenge tables into your local MySQL database.

Run the solution file:

mysql -u your_username -p < solution.sql
